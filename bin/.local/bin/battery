#!/bin/sh

bat=$(acpi -b)
status=$(echo "$bat" | awk '{print $3}' | tr -d ',')

if [ "$status" = "Full" ] || [ -z "$status" ]; then
    exit
fi

percentage=$(echo "$bat" | awk '{print $4}' | tr -d ',')

echo "  BAT: $percentage  "
